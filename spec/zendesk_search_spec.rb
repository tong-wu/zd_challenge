require "spec_helper"

describe ZendeskSearch do
	describe ".run" do
		it "prints out the welcome script" do
		end

		it "parses commands from stdin when running" do
		end

		context "when command is search" do
			it "calls the run_search method" do
			end
		end

		context "when command is items" do
			it "calls the list_items method" do
			end
		end

		context "when command is fields" do
			it "calls the list_fields method" do
			end
		end

		context "when the command is quit" do
			it "calls the quit method" do
			end
		end
	end
end